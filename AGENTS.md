# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Produto

Este é o **ExpoIndustrialApp** — o app **DO EVENTO** (usado durante as datas da feira).
Não confundir com o **MatchIndustrial365** (comunidade B2B que roda o ano todo) nem com o
**match-web** (painel do organizador). Os três compartilham o MESMO Firestore
(`movie-app-ddda3`). Veja [PRODUCT.md](./PRODUCT.md) para a visão completa do ecossistema.
